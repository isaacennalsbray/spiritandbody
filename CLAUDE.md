# Spirit and Body — Claude Guidelines

## Git workflow

- Always create a new branch from main before making any changes (e.g. `claude/brief-description`)
- Never commit directly to main
- Open a PR against main when work is complete
- This keeps separate features and sessions isolated so they don't clash
